// Importa las rutas del módulo Auth
import routes from './routes';

// Importa el store del módulo Auth (si lo tienes)
import store from './store';

// Exporta tanto las rutas como el store
export default {
  routes,
  store
};
